
lcm.datasource.url=jdbc:postgresql://localhost:5432/msu_gensan
lcm.datasource.username=postgres
lcm.datasource.password=dev@icto
lcm.datasource.driver-class-name=org.postgresql.Driver
# Hibernate ddl auto (create, create-drop, validate, update)
lcm.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL81Dialect
#auto-generated SQL queries and the order in which they were executed for debugging purposes.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


logging.file.path=.
logging.file.name=logs/log
logging.level.org.hibernate.SQL=DEBUG

spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER